#include <stdio.h>
#include <stdio.h>

void main(void){
    int arr[10] = {12, 53, 54, 13, 64, 43, 95, 38, 9, 10};
    for(int i = 0; (i < sizeof(arr)/sizeof(int))/2; i++){
        
    }
    for(int j = sizeof(arr)/sizeof(int); j < sizeof(arr)/sizeof(int) -1; j++){}
}